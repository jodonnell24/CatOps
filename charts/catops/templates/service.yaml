apiVersion: v1
kind: Service
metadata:
  name: {{ include "catops.fullname" . }}
  labels:
    {{- include "catops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "catops.selectorLabels" . | nindent 4 }}
