apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "catops.fullname" . }}-test-connection"
  labels:
    {{- include "catops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "catops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
